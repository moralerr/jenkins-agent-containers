@Library('pipeline-commons') _

pipeline {
    agent {
        label 'standalone'
    }
    stages {
        stage('Build/Push Image') {
            steps {
                script {
                    println 'Building image'
                }
            }
        }
    }
}
